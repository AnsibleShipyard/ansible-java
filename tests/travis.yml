---
# travis test file

- hosts: localhost
  connection: local
  sudo: yes
  
  roles:
    - ansible-java

  tasks:
    # Test for java
    - name: Check java
      command: java -version
      register: java_version

    - debug: 
        msg: "{{java_version.stdout}} was installed."
